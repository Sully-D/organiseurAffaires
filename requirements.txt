# Runtime dependencies for the desktop and Django web applications.
PySide6>=6.5
SQLAlchemy>=2.0
Django>=4.2,<7.0

# Optional: uncomment if you need to build the Windows executable from organiseur.spec.
# pyinstaller>=6.0
